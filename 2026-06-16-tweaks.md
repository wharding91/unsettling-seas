# UI Tweaks — 2026-06-16

Two pending visual changes for testing. Neither has been applied to the codebase yet.

---

## 1. Center the sidebar header text

**File:** `_sass/_ed.scss`  
**Target rule:** `.sidebar-item` (line ~445)

Current:
```scss
.sidebar-item {
  padding: 1rem;
}
```

Change to:
```scss
.sidebar-item {
  padding: 1rem;
  text-align: center;
}
```

The sidebar header text is rendered via `{{ site.description }}` inside a `<p>` tag in `_includes/sidebar.html`. Adding `text-align: center` to `.sidebar-item` is sufficient — no HTML change needed.

---

## 2. Replace the horizontal writer list with a vertical alphabetical list

**File:** `index.html`  
**Target:** the `<ul class="horizontal-list">` block (lines 7–16)

The writers are already in alphabetical order by last name (Adisa, Brand, Herrera, Morejón, Philip, Ríos, Trouillot). Replace the `horizontal-list` `<ul>` with a plain `<ul>`:

```html
<ul>
  <li><a href="writers/adisa">Adisa, Opal Palmer</a></li>
  <li><a href="writers/brand">Brand, Dionne</a></li>
  <li><a href="writers/herrera">Herrera, Georgina</a></li>
  <li><a href="writers/morejon">Morejón, Nancy</a></li>
  <li><a href="writers/philip">Philip, M. NourbeSe</a></li>
  <li><a href="writers/rios">Ríos, Soleida</a></li>
  <li><a href="writers/trouillot">Trouillot, Évelyne</a></li>
  <li>Complete Bibliography for All Writers</li>
</ul>
```

Notes:
- Names are inverted (Last, First) to reinforce the alphabetical-by-last-name convention. Revert to natural order if Warren prefers `Opal Palmer Adisa` style.
- The `horizontal-list` CSS rule in `_sass/_ed.scss` (lines 939–970) can be left in place or removed — it will simply be unused if no element carries that class.
- If you want the list styled (e.g., no bullets, larger font), add a scoped class instead of relying on browser default `<ul>` styles.
